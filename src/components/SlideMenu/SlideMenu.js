import React from 'react';
import classes from './SlideMenu.module.css'

const slideMenu = (props) => {
    return (
        <div className={`${classes.SlideMenu}`}>
        
        </div>
    )
}

export default slideMenu;

//${classes.SlideMenuOpen}